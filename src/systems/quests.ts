/**
 * TOMBSTONE — the starter quest chain is gone.
 *
 * Five hand-written errands used to live here (five snakes, six skeletons,
 * twenty stone, build a forge, three minotaurs) and they were the only thing
 * in the QUEST LOG besides the Time Sage's chain. They were removed with the
 * board rewrite: the log is the Sage's now, and repeatable hunting lives on
 * Grizelda's board in `tasks.ts`, which counts out of the kill ledger.
 *
 * The file stays as an empty module rather than being deleted because the repo
 * is updated through the GitHub web UI, which uploads but does not remove. It
 * is imported by nothing. Delete it from the repo whenever convenient — the
 * build does not care either way.
 */
export {};
